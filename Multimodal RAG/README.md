# 🧠 Multimodal RAG (Retrieval-Augmented Generation)

A **Multimodal RAG system** that allows you to ask questions about **PDF documents containing text + images + tables**.  
It extracts and processes content, stores embeddings in a vector database, and retrieves the
most relevant context to generate accurate answers using an LLM.

---

## 🚀 Features

✅ Extract text, tables, and images from PDF files  
✅ Convert images into captions using Vision models (OCR / Image-to-text)  
✅ Chunk and embed text for efficient retrieval  
✅ Store embeddings in a vector database (FAISS / Chroma)  
✅ Query using natural language and generate answers using LLM  
✅ Supports multimodal retrieval (Text + Image Context)

---



